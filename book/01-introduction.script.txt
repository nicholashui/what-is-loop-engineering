In this chapter, you'll learn what this book sets out to teach, who it's for, and the specific learning objectives you'll achieve by the end. You'll also get a first, plain-language picture of what a Loop is, how it differs from prompting an AI coding agent by hand, and the core components that later chapters will unpack in depth.

So let's start with why this book exists. Most engineers meet AI coding agents the same way: a chat box, a prompt, a response, and then another prompt. You stay in the driver's seat, steering the agent one turn at a time. That works, but it puts a hard ceiling on what you can accomplish, because your attention becomes the bottleneck. Every step forward needs you to read the output, decide what's wrong, and type the next instruction.

Loop Engineering is the discipline that removes that bottleneck. Instead of prompting an agent manually, turn by turn, you design a small control system, a Loop, that prompts the agent for you. The Loop discovers work, runs the agent in an isolated workspace, checks the result, feeds problems back in, remembers what has happened, and decides on its own whether to keep going or stop. Your job shifts from typing prompts to designing the system that does the prompting.

This book teaches you how to think about, reason through, and build that system. It assumes you already use AI coding agents at a user level, and it takes you from there to designing autonomous Loops you can trust with long, iterative work.

Now, what will you actually be able to do once you've finished? Each objective is delivered by one or more specific chapters, so you can always trace a goal back to the material that fulfills it.

First, you'll be able to explain what a Loop is and how it differs from manual prompting. That's delivered by Chapter 3, and previewed a little later in this chapter.

Second, you'll be able to place Loop Engineering in its lineage and relate it to the ReAct pattern, that's Reason plus Act. You'll understand how the field grew from prompt engineering, through context engineering, to agent harness engineering, and finally to Loop Engineering, and how a Loop relates to that ReAct pattern. That's covered in Chapter 4.

Third, you'll be able to recount the origins of the term and the precursor known as the Ralph Loop. You'll know where the term came from, who shaped it, and how that simple Ralph Loop technique evolved into production-grade Loop Engineering. That's in Chapter 5.

Fourth, you'll be able to identify and explain the purpose of each Loop component. You'll be able to describe all eight building blocks, which are automation and triggers, isolation, the generator agent, the evaluator, state and memory, skills and knowledge, connectors, and the stopping condition, and give a concrete example of each. That's delivered by Chapter 6.

Fifth, you'll be able to trace the end-to-end Loop lifecycle and its decision points, walking the ordered flow from triggering work all the way to deciding whether to continue, spawn a sub-agent, or stop. That's Chapter 7.

Sixth, you'll be able to build a minimal Loop and evolve it toward a production-grade one. You'll start from a simple bash-style loop and grow it into something with isolation, a separate evaluator, persisted state, and explicit stopping conditions. That's the practical work in Chapter 8.

Seventh, you'll be able to judge when to apply Loops and weigh their benefits. You'll recognize the categories of work, like refactoring, feature implementation, and debugging, where Loops really pay off, and you'll be able to articulate why. That's Chapter 9.

And eighth, you'll be able to anticipate the risks of Loops and apply concrete mitigations. You'll learn to guard against runaway costs and low-quality output, and understand where human engineering judgment remains essential. That's Chapter 10, with the closing synthesis in Chapter 11.

If you can do all eight of those things, you'll have moved from operating an AI coding agent to engineering the system that operates it for you.

Before we go further, let's build a first picture. What exactly is a Loop? The chapters ahead develop each idea carefully, but it helps to carry a working mental model from the start. Three questions frame the whole book, and this section answers each one just enough to orient you.

First, what a Loop is. A Loop is an orchestration system, a small amount of software and configuration, that drives an AI coding agent through repeated cycles of work without a human prompting each step. It is not the agent itself. The agent is one part of the Loop. The Loop is the surrounding machinery that decides what the agent should do, runs it, judges the result, and repeats until the work meets a defined goal or hits a defined limit. The name is literal: the system loops. It produces output, evaluates that output, and uses the evaluation to drive the next iteration, over and over, the way you would if you were supervising the agent yourself, except that the Loop does the supervising.

Second, how it differs from manual prompting. With manual, turn-by-turn prompting, a human is the control system. You read each response, hold the goal in your head, judge whether the output is good enough, and craft the next prompt. Progress stops the moment you step away. With a Loop, that control system is designed once and then runs on its own. The decisions you used to make in your head, things like, is this good enough, what should happen next, are we done, become explicit, encoded rules: an evaluator that checks the output, a state file that remembers progress, and a stopping condition that ends the run. The practitioner's role shifts from operator to system architect. You design the loop; the loop does the prompting.

Third, the core components. Every Loop, from the simplest to the most elaborate, is assembled from the same small set of building blocks. You'll study each one in depth in Chapter 6, but here's the cast of characters so the rest of the book reads as a coherent whole. There's automation, or the trigger, which decides when and how work enters the Loop, for example a schedule, an event such as a new issue, or a command you run. There's isolation, often through worktrees, which keeps each agent run separate and reversible so mistakes don't contaminate your main workspace. There's the generator agent, which is the AI coding agent that actually produces the output. There's the evaluator, which verifies that output by running tests, reviewing code, or scoring against a rubric. There's state, or memory, which persists progress across iterations so the Loop remembers what has been done. There's skills and knowledge, which supply reusable guidance and standards the agent should follow. There are connectors, which integrate the Loop with external systems such as source control, issue trackers, and chat platforms. And finally there's the stopping condition, the rule that ends the Loop, such as tests passing, a quality threshold, or a budget limit.

Hold this picture loosely for now. The point is simply that a Loop is built from parts, that those parts replace the judgments you'd otherwise make by hand, and that designing them well is the craft this book teaches.

Let's also look at how the rest of the book is organized. The chapters follow a deliberate arc. The early chapters establish what and why: who the book is for in Chapter 2, a precise definition of the discipline in Chapter 3, its lineage and intellectual roots in Chapter 4, and its origins in Chapter 5. The middle chapters dissect the parts and how they interact: the eight components in Chapter 6, and the end-to-end lifecycle in Chapter 7. The practical chapter shows you how to build one, that's Chapter 8. The closing chapters cover when to use Loops, their benefits and risks, and a final synthesis, across Chapters 9 through 11. A glossary and a references section round out the book. You can read straight through for a complete course, or jump to a specific topic if you prefer.

So to recap the main ideas. Loop Engineering is the practice of designing a control system, a Loop, that prompts and orchestrates AI coding agents autonomously, instead of prompting them manually one turn at a time. A Loop is the orchestration system, not the agent; the agent is just one component within it. The defining difference from manual prompting is who controls the cycle: with a Loop, the judgments a human would make by hand become explicit, encoded rules, which shifts your role from operator to system architect. Every Loop is assembled from the same core components: automation and triggers, isolation, a generator agent, an evaluator, state and memory, skills and knowledge, connectors, and a stopping condition. And this book has eight explicit learning objectives, each delivered by specific chapters, taking you all the way from understanding a Loop to building and judging one.
