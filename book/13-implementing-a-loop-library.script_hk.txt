喺呢一章，你會學識點樣建造一個屬於自己嘅 loop 庫。佢係一個經過策展、納入版本控制嘅可重用 loop 定義集合，俾你嘅團隊去瀏覽、複製同運行。我哋會定義一個單一嘅 loop 條目 schema，令每一個 loop 都用同一種方式去描述。我哋會鋪排一個儲存佢哋嘅儲存庫結構。我哋會展示點樣令個庫可被搜尋。而最重要嘅係，我哋會展示點樣用「建造」嗰一章嘅機制，將一個靜態嘅庫條目變成一個運行中嘅 loop。讀完之後，你就有能力攞上一章嗰四十五個形態，或者你自己嘅，將佢哋變成一個可執行嘅庫。

噉就由分辨「目錄」同「庫」講起。上一章係一個目錄，一次唯讀嘅導覽，睇其他人寫嘅 loop。而一個 loop 庫係更強嘅嘢。佢係你自己儲存庫入面一件活生生嘅產物，當中每一個條目都係結構化資料，而唔係散文，於是佢可以被搜尋、被驗證、被版本化，並且——最關鍵嗰步——被執行。呢個分別，同本書一直返嚟覆去講嗰個一樣。散文描述一個 loop。而一個庫就定義佢，精確到足以令一部機器運行得起。

實作一個庫，意味住要做三個決定，然後將佢哋接駁埋一齊。第一，一個 schema，一種一致嘅形狀去描述每一個 loop，令啲條目可以互相比較、又可被機器讀取。第二，一個儲存，啲條目住喺邊度，以及佢哋點樣被組織、被版本化、被審查。第三，一個 runner，一個庫條目點樣變成一個由八個組件砌成嘅運行中 loop。

等我哋按呢個次序逐個講，先由 schema 開始。一個庫嘅基礎，就係一份大家一致同意嘅描述，講清楚一個 loop 係乜。請留意，上一章每一個條目都隱含噉答咗同樣嘅問題：邊個寫、佢做乜、佢點樣運作、佢幾時停。將呢啲問題變成明確嘅欄位，你就有咗一個 schema。一個好嘅 schema 會映照返八個組件，令一個庫條目同一個可運行嘅 loop 一對一噉對應。

實際嘅形式，就係一個 Markdown 檔案加 YAML frontmatter。佢俾人睇得明，又俾機器解析得到，而呢個正正就係一個庫條目所需要嘅嘢。個 frontmatter 帶住身分同發現嘅欄位，例如 id、name、author 同 category，然後就係 loop 契約本身：觸發器、生成器、評估器、隔離策略、狀態檔案、停止條件、連接器同技能。呢啲正正就係八個組件。所以一個庫條目，就係一份完整、宣告式嘅 loop 描述。關於點樣運行佢嘅任何嘢，都唔會住喺呢個檔案以外。

有兩個設計原則令呢樣嘢行得通。第一，每一個組件都係一個欄位，於是個條目就係嗰份契約。第二，prompt 係一個引用，而唔係內嵌文字。實際嘅 agent 指令住喺一個獨立嘅 prompt 檔案，咁樣可以令 prompt 跨 loop 重用，並且可以獨立噉編輯。

唔係每一個 loop 都需要每一個欄位，但有一小撮核心欄位應該係強制嘅，咁個庫先永遠唔會含有一個運行唔到嘅條目。id、name、author 同 category 係必需嘅，用嚟做身分同發現。purpose 係必需嘅，作為一句話、可被檢查嘅意圖。生成器嘅 prompt 檔案係必需嘅，因為冇生成器就冇 loop。而停止條件係必需嘅，因為一個無界限嘅 loop 係一個缺陷，唔係一個條目。評估器就強烈建議要有，因為冇佢，「完成」就只係 agent 自己講。最重要嘅一條規則就係：冇任何條目可以缺少停止條件。呢一條約束，正正就係令一個由眾多自主 loop 組成嘅庫，唔會變成一個由眾多失控進程組成嘅庫嘅關鍵。

而家講儲存。定咗 schema 之後，儲存就好直接：一個喺版本控制之下、由條目檔案組成嘅目錄。將純檔案放入 git，你就免費得到歷史、審查、分支同 diff，而呢啲特性，正正就係令 loop 本身值得信任嘅同一批特性。個結構係一個 loop 一個檔案，按類別分資料夾，映照返上一章嗰五大類。prompt、評分準則同技能都係共享、第一等公民嘅檔案，於是同一個評分準則可以支撐好多個 loop。而一個可被機器檢查嘅 schema 就住喺儲存庫入面，於是 CI 可以喺每一次改動時驗證每一個條目。呢個就係個庫將一個評估器套用喺自己之上。

個庫應該以佢所宣揚嘅標準去要求自己。一個喺合併前對照 schema 去驗證每一個條目嘅檢查，係最低門檻。佢執行三條不可妥協嘅嘢：一個有效嘅形狀、一個存在嘅停止條件，以及一個存在嘅 prompt 檔案。將佢放入 CI，個庫就永遠唔可以合併一個運行唔到、或者會永遠運行落去嘅條目。

跟住，令佢可被搜尋。一個庫，只有當人哋可以快速搵到啱用嗰個 loop 嗰陣，先至無愧於佢個名。因為每一個條目都帶住結構化嘅 frontmatter，所以搜尋大致上就係讀取嗰啲元資料、再將佢投射成可瀏覽嘅嘢嘅問題。兩個低成本嘅機制，幾乎涵蓋咗所有需要。有一個生成嘅索引：一個細小嘅腳本讀取每一個條目嘅 frontmatter，寫出一個分組、有連結嘅 README，就好似上一章嗰個目錄噉。仲有分面、基於標籤嘅篩選：因為 category、tags 同 maturity 都係結構化欄位，你毋須一個資料庫都可以篩選。嗰啲元資料就係個搜尋索引。

而家講嗰一步——分隔一個真正嘅庫同一個花俏筆記資料夾嘅地方——將一個庫條目變成一個運行中嘅 loop。一個庫條目係一份宣告。運行佢，就意味住將嗰份宣告交俾一個 runner，由佢去組裝八個組件、並驅動生命週期。佢就係「建造」嗰一章嗰個一模一樣嘅生產級 loop，只係而家由條目去參數化，而唔係寫死。

runner 嘅工作係一次忠實嘅翻譯，逐個欄位嚟。觸發器決定幾時開始。隔離策略每次嘗試喺一個用完即棄嘅分支上建立一個 git worktree。生成器嘅 prompt 檔案同輸入去渲染 prompt 並呼叫 agent。評估器運行客觀關卡，然後評論者或者評分準則關卡。狀態檔案持久化進度，並將佢循環入下一個 prompt。而停止條件就決定係要繼續、合併並停止，定係升級。每一個都直接對應到生命週期嘅一個階段。

重點唔係呢個特定嘅腳本。重點係嗰份分離。runner 只寫一次，並包含咗所有嘅編排。庫條目就只包含宣告。加多一百個 loop，runner 都唔使改。呢個就係 schema 帶嚟嘅回報。一個 loop 變成咗資料，而運行任何一個 loop，都係同一條程式碼路徑。

呢度亦都係參數化 loop 出場嘅地方。好多目錄 loop 都有空格，例如「N 次成功之後停」、「repository task」，或者「quality threshold」。一個庫令呢啲空格變成第一等公民嘅輸入，喺運行時供應。於是你可以將同一個條目針對唔同目標運行，毋須改動條目本身。一個參數化得宜嘅條目，可以取代成打近乎重複嘅複本。呢個就係一個庫點樣喺增長之中保持細小嘅方法。你加嘅係參數，而唔係分叉。

噉，你點樣去到你第一個庫呢？你毋須事前就建好成個庫，就好似你喺「建造」嗰一章都唔係事前建好成個 loop 一樣。用同樣嘅減法方式去培育佢。第一，晉升一個行得通嘅 loop：攞一個你已經運行緊嘅 loop，用 schema 寫出嚟，並將佢作為第一個條目提交。第二，runner 只寫一次，方法係將你現有嘅編排器推廣。第三，將 schema 驗證器加入 CI，咁個庫就唔可以接受一個破損或者無界限嘅條目。第四，只有喺你真正重用之際先晉升 loop。第五，生成索引，令個庫可被瀏覽。紀律同本書其他地方一樣：當「冇將一個 loop 放入個庫」會造成真正嘅重複嗰陣，先將佢加入，唔好早過呢個時候。

噉等我哋將啲關鍵諗法收攏埋一齊。一個 loop 庫將個唯讀目錄，變成一件活生生、可執行嘅產物，當中每一個 loop 都變成結構化、納入版本控制嘅資料，而唔係散文。實作一個，意味住要將三個決定接駁埋一齊：一個 schema，每一個 loop 一種形狀；一個儲存，納入版本控制嘅檔案；以及一個 runner，執行任何條目。

個 schema 應該令每一個組件都成為一個明確嘅欄位，並將 prompt 以引用檔案嘅形式儲存，而且冇任何條目可以缺少停止條件。個儲存係一個 git 目錄，由「一個 loop 一個檔案」、按類別分資料夾嘅條目組成，配上共享嘅 prompt、評分準則同技能，以及一個喺 CI 中驗證嘅 JSON Schema。搜尋幾乎免費噉由結構化嘅 frontmatter 得來。個 runner 只寫一次，將一個條目嘅欄位翻譯到生命週期上。佢就係嗰個生產級 loop，只係將佢嘅常數換成由條目讀取，於是加 loop 永遠唔會改動編排。參數化嘅輸入將目錄嗰啲空格變成運行時嘅參數。而你就用減法去建個庫：晉升一個行得通嘅 loop、寫個 runner、加入 CI 驗證，然後只有喺重用真正有需要嗰陣先加條目。
